version: '3.5'

services:
  apache:
    image: "httpd:${APACHE_VERSION}"
    volumes:
      - ${APACHE_CONFIG}:/usr/local/apache2/conf/httpd.conf
    ports:
      - "8180:8180"
    networks:
      - network1

networks:
  network1:
    name: apache_network
